setenv autostart yes
if fatload mmc 1:2 $kernel_addr_r mini_visor; then
	if fatload mmc 1:2 $fdt_addr_r u-boot.dtb; then
	    bootelf $kernel_addr_r $fdt_addr_r $kernel_addr_r
	else
	    echo "Unable to read u-boot.dtb"
	fi
else
	echo "Unable to read mini.elf"
fi
sleep 10000000
